<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wanmait.exam.mapper.QuestionAnalysisMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wanmait.exam.entity.QuestionAnalysis">
        <id column="id" property="id" />
        <result column="question_id" property="questionId" />
        <result column="question_analysis" property="questionAnalysis" />
        <result column="input_time" property="inputTime" />
        <result column="user_id" property="userId" />
        <result column="enable" property="enable" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, question_id, question_analysis, input_time, user_id, enable
    </sql>

</mapper>
